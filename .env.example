# ════════════════════════════════════════════════════════════════
# 오늘비 환경 변수.
#
# 핵심 예보 화면은 키가 하나도 없어도 완전히 동작합니다:
#   Open-Meteo(예보) + RainViewer(레이더 접근 신호)는 키 불필요.
# 지역 직접 검색과 현재 위치 지명 표시에는 KAKAO_REST_API_KEY가 필요합니다. 나머지 키는 보강
# (enrichment)용이며, 없거나 실패하면 해당 기능만 우아하게 강등됩니다.
#
# 사용법:  install -m 600 .env.example .env.local  →  값 입력  →  dev 서버 재시작
# 모든 키는 서버 전용이며, NEXT_PUBLIC_ 접두어가 없는 한 클라이언트에
# 노출되지 않습니다(현재 NEXT_PUBLIC_ 변수는 없음).
# ════════════════════════════════════════════════════════════════


# ─── 대한민국 행정구역 검색 (지역 직접 찾기에 필요) ───

# Kakao Map Local REST API 키. 서버에서만 사용되며 클라이언트에 노출되지 않습니다.
# 미설정 시 브라우저 현재 위치는 계속 동작하지만, 지명 대신 '현재 위치'로만 표시되고
# 지역 직접 검색은 search_not_configured로 비활성화됩니다(재시도 버튼을 띄우지 않음).
#   (read: lib/locationSearch.ts)
KAKAO_REST_API_KEY=


# ─── 예보 소스 (선택적 교차 검증 · Open-Meteo가 키 불필요 기본 워크호스) ───

# MET Norway (Locationforecast) — 예보 비교에는 더 이상 쓰이지 않습니다. 한국에는
#   probability_of_precipitation을 발행하지 않아(노르딕 전용) 채점이 불가능하고,
#   lib/reliability/ 파이프라인만 이 소스를 읽습니다. 아래 키는 그 파이프라인 전용.
#   무료·키 불필요. 단, MET 이용약관상 User-Agent에
#   실제 연락처(이메일 또는 URL)가 반드시 포함되어야 합니다. 연락처가 없으면 MET가
#   HTTP 403을 반환하고 provider는 needs-config로 보고되어 호출되지 않습니다.
#   형식: "AppName/version 연락처".  (read: lib/providers/met-norway.ts:25)
MET_NO_USER_AGENT=오늘비/1.0 you@example.com

# 기상청 단기예보 (data.go.kr 활용신청). Encoding/Decoding 키 어느 쪽이든 동작.
#   미설정 → KMA 예보/관측 보강 생략.  (read: lib/providers/kma.ts:50)
KMA_SHORT_TERM_API_KEY=

# Pirate Weather (pirateweather.net 무료 키). 미설정 → 해당 소스 생략.
#   (read: lib/providers/pirate-weather.ts:27)
PIRATE_WEATHER_API_KEY=

# WeatherAPI.com (weatherapi.com 무료 키). 미설정 → 해당 소스 생략.
#   (read: lib/providers/weather-api.ts:21)
WEATHERAPI_KEY=


# ─── 대기질 (선택) ───

# AirKorea (data.go.kr 한국환경공단_에어코리아_대기오염정보).
#   미설정 → Open-Meteo 대기질(키 불필요)로 자동 폴백, 그다음 null.
#   haze/가시거리에 미세하게만 반영 — 경고/의료성 카피는 절대 없음.
#   (read: lib/providers/air-quality.ts:62)
AIRKOREA_API_KEY=


# ─── 기상특보 (선택) ───

# 기상청 기상특보 (data.go.kr) — 위 단기예보 키와는 별개 서비스.
#   미설정 → 특보는 []로 강등(예보 확률로 지어내지 않음).
#   (read: lib/providers/kma.ts:55)
KMA_WARNING_API_KEY=


# ─── 레이더 영상 (KMA API허브 — 선택) ───

# KMA API허브 레이더 authKey (apihub.kma.go.kr — HSR 500m 반사도).
#   레이더 섹션의 강수 에코를 서버에서 CARTO 베이스맵 위에 렌더링합니다.
#   키와 ~13MB 격자는 클라이언트에 절대 도달하지 않고 작은 에코 PNG만 전송.
#   미설정 → 레이더 섹션은 베이스맵 + 한글 라벨만 표시(빈 상태), 나머지는 영향 없음.
#   요청한 프레임은 최근 관측 창으로 제한되고, 서버 프로세스마다 최대 2개 렌더와
#   대기 8개까지만 처리합니다. 생성한 변경 불가 PNG는 브라우저/CDN에서 1일 캐시될 수
#   있지만 서버 메모리 캐시는 프로세스별·일시적입니다. 키가 없어도 나머지는 동작합니다.
#   (read: lib/radar/delivery.ts)
KMA_APIHUB_KEY=

# ─── 멀티소스 강수 합의 (기본 ON · 비상시 opt-out) ───

# 비상 중지 스위치("0"이면 비활성). Sky 스냅샷 조립기의 일별 POP/predicted_mm은
#   여러 예보 소스의 학습 가중 합의로 산출됩니다. "0" → 기존 단일 Open-Meteo
#   출력 그대로(byte-for-byte). 그 외 값/미설정은 학습 합의를 활성화합니다.
#   ※ /api/sky가 삭제되어 이 조립기는 더 이상 서비스되지 않습니다. 따라서 이 값은
#      현재 어떤 응답에도 영향을 주지 않습니다(모듈 존치 여부는 #62에서 결정).
#   (read: lib/liveSkySnapshot.production.ts:45)
MULTI_SOURCE_PRECIP=

# ─── 강수 신뢰도 학습 (배치 학습 + 웹 런타임 읽기) ───
# 매일 배치(.github/workflows/precip-reliability.yml → scripts/precip-reliability.ts)가
# 하나의 트랜잭션으로 예보 스킬을 채점하고 공개 reliability-state 브랜치에 영속 저장합니다.
# main/release 브랜치는 생성 상태를 추적하지 않으며 state 커밋은 Vercel 배포를 만들지 않습니다.
# 웹 런타임은 가중치 JSON만 HTTP로 읽고 스키마 검증 후 사용합니다.
# 자세히: lib/reliability/README.md.

# 웹 런타임용 가중치 JSON URL은 이 저장소 reliability-state 브랜치의 raw URL로
# 코드에 고정됩니다. HTTP 실패/잘못된 스키마는 마지막 정상 캐시 또는
# 동일가중치 폴백으로 강등됩니다.

# 지상 관측 진실값 키: 기상청 지상(ASOS) 일자료 조회서비스(data.go.kr
#   AsosDalyInfoService) — 단기예보 키와는 별도 구독. 미설정 시 KMA_SHORT_TERM_API_KEY로
#   폴백하나, 그 키가 ASOS 미구독이면 403 → 해당 일자 채점 건너뜀(값 지어내지 않음).
#   cron/스크립트 전용.  (read: lib/reliability/groundTruth.ts:22)
KMA_OBSERVATION_API_KEY=

# Hedge 학습률 η(선택). 클수록 가중치가 빠르게 이동. 기본값 0.5. cron/스크립트 전용.
#   (read: lib/reliability/weights.ts:35)
RELIABILITY_ETA=

# 저수준 로컬 파일 어댑터용 상태 디렉터리(선택). 기본값: ./data/reliability.
#   예약 작업은 격리된 임시 후보 디렉터리를 직접 사용하므로 이 값을 읽지 않습니다.
#   Vercel 웹 런타임도 이 경로를 읽지 않습니다(raw GitHub URL 사용).
#   (read: lib/reliability/persistence.ts)
RELIABILITY_DATA_DIR=


# ─── 전국 지역별 강수 성능 (PostgreSQL · 선택) ───

# 06시/18시 KST 고정 코호트의 익일 예보와 ASOS 관측을 저장하는 표준 PostgreSQL URL.
# 미설정 시 예보는 그대로 동작하며 지역별 최근 성능 보강만 비활성화됩니다.
# 사용자 위치는 이 DB에 저장하지 않고, 공식 관측소·예보 캡처·관측값만 저장합니다.
# 예약 작업: .github/workflows/local-performance.yml.
PERFORMANCE_DATABASE_URL=

# 저장소 어댑터 계약 테스트(lib/performance/storeContract.ts)를 PostgreSQL에도
# 돌리기 위한 "버려도 되는" 데이터베이스 URL. 미설정 시 해당 테스트는 skip으로
# 보고되고 인메모리 어댑터만 검증됩니다.
#   경고: 이 스위트는 해당 DB의 테이블을 TRUNCATE합니다. 절대 운영 URL을 넣지 마세요.
#   예: createdb raintoday_store_contract 후
#       postgres://<user>@localhost:5432/raintoday_store_contract
#   (read: lib/performance/storeContract.test.ts)
PERFORMANCE_STORE_CONTRACT_URL=


# ─── 디버그 (선택) ───

# Sky 스냅샷 조립기는 고급 진단 화면용 비밀 없는 precipLearning 요약을 항상 제공합니다.
# "1"이면 같은 런타임 결정을 원시 precipWeighting 디버그 블록으로도 추가 노출합니다.
# ※ /api/sky가 삭제되어 이 조립기는 더 이상 서비스되지 않으므로, 이 값도 현재 어떤
#    응답에도 영향을 주지 않습니다(모듈 존치 여부는 #62에서 결정).
# 프로덕션에서는 미설정 권장.  (read: lib/liveSkySnapshot.production.ts:46)
RELIABILITY_DEBUG=
